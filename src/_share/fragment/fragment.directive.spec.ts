import { FragmentDirective } from './fragment.directive';

describe('FragmentDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentDirective();
    expect(directive).toBeTruthy();
  });
});
