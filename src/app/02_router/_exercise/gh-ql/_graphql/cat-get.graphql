query getCat($id: String!) {
  cat(id: $id){
    id
    name
    age
    breed
  }
}

query getCats($offset:Int, $limit: Int) {
  cats(offset: $offset, limit: $limit) {
    id
    name
    age
    breed
  }
}
